function setup() {
  createCanvas(600, 300);
  background(0); // black background
  textFont('Georgia');
  textSize(48);
  fill(255);
  textAlign(CENTER, CENTER);
  text('Still Cool 😎', width / 2, height / 2);
}

function draw() {
  // nothing here
}